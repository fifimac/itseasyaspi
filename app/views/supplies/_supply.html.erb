<%= form_for supply, remote: true do |f| %>
  <%= f.check_box :bought %>
  <%= f.submit "Update" %>
  <%= f.label :bought, supply.name, :class=>"listLabel" %>
  <%= link_to "(remove)", supply, method: :delete, data: {confirm: "Are you sure?"} , remote: true%>
<% end %>

